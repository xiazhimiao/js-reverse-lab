# 实用工具

公共工具脚本，供各逆向案例复用。

## 工具列表

### 吐环境代理脚本.js

> 用途：通过 `Proxy` 拦截浏览器环境对象（`window`、`navigator`、`document` 等）的属性读写，用于定位目标 JS 检测了哪些环境属性。

> 使用方式：将需要监控的对象名传入 `proxy_array`，运行脚本即可在控制台输出所有属性访问记录。

---

> *后续新增工具请在下方追加*
